/**
 * Meta DM adapter (Instagram/Facebook) — STUB ONLY. Not built this session.
 *
 * Scope: P2. Blocked on Meta business-messaging permissions / app review —
 * do not start building this until that's cleared. See the session scope
 * doc for context.
 *
 * When built, this file's job is the same as channels/sms/adapter.js:
 * translate a Meta Messenger/Instagram DM webhook payload into a normalized
 * message via lib/normalize.js's makeNormalizedMessage(), with
 * channel: 'meta_dm'. Core and responders require zero changes.
 */

'use strict';

module.exports = {};
