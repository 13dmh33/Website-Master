# Secrets
.env.local

# Dependencies
node_modules/

# Runtime data — generated by agents at run time, not committed
leads/*.json
queue/*.json
messages/*.json
messages/*.txt
mockups/*.txt
state.json

# Logs
logs/*.log

# OS
.DS_Store
