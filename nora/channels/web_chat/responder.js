/**
 * Web chat responder — STUB ONLY. Not built this session.
 *
 * Scope: P1, next session. See channels/web_chat/adapter.js and the session
 * scope doc for context.
 *
 * When built, this file's job is the same as channels/sms/responder.js:
 * format core's decision into the chat widget's outbound reply format and
 * send it through lib/dispatch.js's dispatchOutbound(), gated by a new
 * CHAT_LIVE check (already reserved in lib/gates.js).
 */

'use strict';

module.exports = {};
