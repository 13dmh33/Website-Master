# Claude Code session: Clear the 436-lead checked-but-unsent backlog — run Pitcher daily (already scheduled) or raise the daily cap if speed matters more than pacing — quick wins only

**Branch:** `feature/clear-send-backlog-quick-wins` (off `main` — this repo has no `master` branch)
**Target usage:** light — a day with less available capacity. Estimated queue: ~1.25h.
**Generated by:** Merlin, 2026-07-19 nightly run. Advisor-generated — review before running; Merlin recommends, it does not act.

---

## Session structure (standing rules)

- Ordered priority queue, worked top-down. No fixed task count — do as much as capacity allows.
- Atomic tasks, one commit per task. Never leave a mid-change state across a stop point.
- Living `CHECKPOINT.md`, updated after every task, so any stop is safe.
- Phase 0 state audit first: verify each queue item's premise is still true before starting it (repo state may have moved since this prompt was generated) — mark done/partial/not-started, skip what's already done.
- Graceful stop, not a percentage: when running low, finalize `CHECKPOINT.md` and stop cleanly rather than starting a task that can't be finished and committed.

---

## Priority queue

1. **Clear the 436-lead checked-but-unsent backlog — run Pitcher daily (already scheduled) or raise the daily cap if speed matters more than pacing** (~0.1h)
   436 leads are already Checker-approved and waiting — every one of them is closer to paid than any lead that doesn't exist yet. At the current 30/day cap this takes 15 days; zero new code required either way. This is currently the single largest lever available.

2. **Add a real STRIPE_SECRET_KEY and replace placeholder Payment Link URLs** (~0.25h)
   The post-call-proposal pipeline is fully built and tested end to end except for this — a real key turns a finished feature into revenue capability with zero code changes.

3. **Investigate the checked -> sent drop-off (436 leads lost, 30% conversion) before building anything new** (~0.5h)
   This is the single biggest measured leak in the funnel. Understanding why (capacity limit vs. genuine drop-off vs. measurement artifact — see the pipeline snapshot's integrity flags) is higher-value than building new lead sources on top of a leaky funnel.

4. **Enable IMAP for dave@trevoadvisors.com in Zoho Mail settings** (~0.1h)
   Unblocks two already-finished pieces of code (Reply Agent, post-call-proposal Drafts delivery) with zero further engineering — pure unlock, no new surface area.

5. **Revert checker-config.json / diagnoser-config.json daily_limit back to 30 (currently elevated)** (~0.05h)
   A one-line config fix, not a build — trivial effort, low but nonzero integrity value.

6. **Tighten DMARC to p=quarantine once clean report cycles confirm it is safe** (~0.25h)
   Deliverability hygiene, not a sales lever — low priority until the trigger condition (clean report cycles) is actually met.

---

## Definition of done

- Phase 0 audit complete.
- Queue worked top-down as far as capacity allows; anything not reached rolls to the next session.
- Living `CHECKPOINT.md` present and current as of the last committed task.
- No emojis, sentence case, "AI agent" not "bot" in any customer-facing string touched.
