# Website-Master — agent handoff, 2026-07-28

Self-contained briefing. Everything below was verified against live state during
the session, not inferred from docs. Where something is unverified it says so.

---

## 1. Read this first: the headline number is an artifact

**"273 leads contacted, 0 replies" is not a finding. It is the absence of one.**

Reply detection has never run. Verified:

| Check | Result |
|---|---|
| `config/reply-agent-config.json` | `current_month: null`, `auto_run: false` |
| `last_uid` high-water mark | never set — reply-agent has never polled |
| `messages/*-sent.json` | 273 records, all with `replies: []`, none non-empty |
| `poller.js` | superseded by reply-agent; not in cron or launchd |
| `webhook.js` | SMS only, and SMS is blocked by A2P anyway |

Nothing has ever written to the `replies[]` field, so `replied: 0` in the funnel
can only ever read zero. There may be six weeks of replies sitting unread in
`dave@trevoadvisors.com`.

**IMAP was the blocker and was enabled 2026-07-27.** Verified working this
session: IMAP connects to `imap.zoho.com:993`, INBOX reachable, `imapflow` and
`mailparser` both installed, `scripts/reply-agent.js` is on `main`.

### The first thing to do

```bash
node scripts/reply-agent.js --dry-run
```

Two minutes. Reads replies from known leads only, drafts into Zoho Drafts,
**sends nothing**. If replies exist, every other priority is wrong. If there are
genuinely zero from 178 delivered emails, that is a real deliverability signal
worth having before sending more.

---

## 2. Recommended order of work

1. **Run reply-agent (above).** Blocks everything else's validity.
2. **Call the 16 approved Austin leads** (`node scripts/caller.js`). Highest-value
   action that depends on no system working.
3. **Schedule drip.** 169 leads are frozen mid-sequence, untouched since
   2026-07-18. Cause found: `deploy/com.trevo.drip.daily.plist` was prepped but
   never installed, and still has `__NODE_PATH__` / `__REPO_PATH__`
   unsubstituted. Installing it starts real outreach — needs Dave's explicit
   go-ahead.

### Explicitly deprioritised

- **`STRIPE_SECRET_KEY`** — Merlin ranks it #1 but is half-wrong (see §4). The
  Payment Links half is done and merged. The key half is gated on reconciling
  the stale `feature/post-call-proposal` branch, so it is not the ~0.25h claimed.
- **Social launch (Molly)** — Dave's own plan calls it Tier 4. The
  @trevoadvisors account does not exist yet.
- **More building.** Seven systems, three advisor agents, ~30 scripts, zero
  closed deals. This session alone found the pipeline about to cold-email a font
  designer, pitch Home Depot, and exceed a spend cap that could not fire.

---

## 3. Standing rules an agent must not violate

- **Never run a branch operation (checkout / stash / merge / rebase) while a
  pipeline process is running.** `state.json` and `config/cost-log.json` are live
  working files. A `git stash` already reverted `state.json` to a stale baseline
  once while Pitcher kept writing real sends on top of it — real data loss.
  Check `ps aux | grep "node scripts/"` first, and back up both files.
- **HVAC is excluded** from all Trevo scouting, pitching, and content — the owner
  works for an HVAC manufacturer. (Techs4Tatas/Miley is exempt; different brand.)
- **Mac-only scripts:** Scout, Pitcher, Drip, Reporter, Webhook, Poller,
  reply-agent, contact-scraper. The container's egress allowlist blocks
  Outscraper, Twilio, Zoho, and arbitrary contractor sites.
- **Review-first content.** Molly and Miley both default `FORCE_QUEUE=1`;
  nothing auto-posts. Nora's `NORA_LIVE`/`SMS_LIVE` default off — every outbound
  becomes a draft file.
- **Techs4Tatas (`miley/`) is a separate brand.** Sibling architecture, different
  context. Do not apply Trevo content decisions to it.

---

## 4. Merlin's latest report, and where it is wrong

Full report: `merlin/reports/2026-07-28/report.md` (+ `session-primary.md`,
`session-light.md`). Ranked recommendation is `add_stripe_key` (score 24.0).

**Verified correct:**
- Funnel byte-identical to 07-26 — genuinely frozen. `scouted 665 / diagnosed 644
  / checked 642 / sent 187 / drip_d1 18 / drip_d1b 1 / replied 0 / hot 0`.
  Biggest drop-off `checked -> sent`, 455 lost, 29.1%.
- 1 unpushed branch — `claude/weekly-email-revenue-plan-4ysmqx`, 1 real commit
  carrying live pipeline data (leads, briefs, cost-log). Genuinely at risk.
- Logged spend $1.42/mo. 2 stale branches. main 0/0 with origin.

**Now wrong:**
| Item | Reality |
|---|---|
| #1 Stripe (score 24.0) | Payment Links half **done, tested in-browser, merged**. Zero placeholders remain. Key half real but branch-gated, not ~0.25h. |
| #3 Enable IMAP (score 18.0) | **Done 2026-07-27.** Will be re-recommended nightly forever — Merlin reads repo state and a Zoho setting leaves no trace. |
| "Untracked: leads-web" | Committed this session. |
| #4 frozen `sent` stage | Hypothesis correct. **Cause found:** drip never scheduled. |

**Root causes, for whoever fixes Merlin:**
- `merlin/lib/repo-facts.js` has exactly three resolvers
  (`revert_elevated_daily_limits`, `merge_nora_and_funnel_metrics`,
  `fix_poller_email_reply_gap`). There is no `add_stripe_key` resolver, so Merlin
  cannot see a placeholder-count change. Split that candidate in two and add one.
- Vendor-side changes (IMAP, Twilio approval, Buffer tokens) are permanently
  invisible to Merlin. The intended mechanism is `merlin/decisions.json`, which
  currently holds exactly one entry. Record one superseding `enable_imap`.
- **Merlin's GitHub Actions workflow has never once succeeded.** Every report on
  `origin` was committed by hand — the `merlin-agent` CI identity has zero
  commits in repo history. Leading cause: `ZOHO_EMAIL` / `ZOHO_APP_PASSWORD`
  missing as GitHub secrets. `run.js` writes report files first, then emails, and
  sets `exitCode = 1` on send failure; the commit step has no `if: always()`, so a
  failed email means no report is ever committed — exactly the observed pattern.
  The failure alert (`notify-workflow-failure.js`) needs those same two secrets,
  which is why it has been silent.
- **Scope limit:** Merlin is Trevo-only. Molly, Milly, Miley, Missy, Reeve and
  Nora are invisible to it. The deprecated-model bug below is exactly the class
  of failure it will never surface.

---

## 5. Bugs found and fixed this session (all pushed to `main`)

All were invisible to the 177-test suite, which passed before the session began.
They surfaced by **running** things, not reading them.

| # | Bug | Severity |
|---|---|---|
| 1 | contact-scraper adopted third-party boilerplate as lead emails — a font foundry, a type designer's Gmail, a WP theme demo address, all from CSS licence headers. All 3 were `scouted` and heading for a real send. | Legal / reputation |
| 2 | Non-prospects queued to be pitched — 8 approved briefs including Ferguson ×2 and Home Depot ×2. `isNonProspectBusiness()` existed but only ran in contact-scraper. | Waste / embarrassment |
| 3 | The $10/mo Outscraper cap was unenforceable. `--budget` skipped the whole monthly check, and `lead-gen.sh` passes it by default. Config showed `current_month: "2026-06"` holding spend accumulated across June *and* July. | Money |
| 4 | Floating-point loss at the budget boundary — `10 - 9.9 = 0.09999999999999964`, so `Math.floor` forfeited one result at every boundary. | Minor |
| 5 | Milly + Miley hardcoded `claude-sonnet-4-20250514`, now a live 404. Miley degraded silently to evergreen; **Milly has no fallback and would crash the whole weekly run.** | Correctness |
| 6 | `isContactSuppressed()` re-read the do-not-contact store from disk on every lead, inside loops in both pitcher and drip. | Efficiency (13ms today; scales) |
| 7 | Molly: 75% of output drove to Instagram DMs Dave does not staff, incl. a CTA burned into rendered images. `generate-evergreen.js` would have regenerated the previously-quarantined content. | Brand |

Test count 73 → 92. Every new test uses real addresses/names from real runs.

**Note on #1:** the fix was tuned against all 107 historical harvested emails.
The first cut also rejected four *genuine* contractors (`adeptplumbing.com` for
"Adept Plumbing", acronym domains like `pduhome.net`). One was a bug in the fix
itself — trade words in the acronym stopword list collapsed "**P**lumber **D**own
**U**nder" to "du". Anyone touching
`scripts/lib/lead-quality.js#isImplausibleBusinessEmail` should re-run that
107-email sweep before and after.

---

## 6. System status

| System | State |
|---|---|
| **Trevo** (root) | Live. ~30 scripts. 665 scouted, 187 sent, 0 confirmed replies (see §1). Checkout wired to real Stripe Payment Links (website-only + Nora; Atlas/Argus removed, not live). |
| **Missy** (`job-hunter`) | **Live.** Worktree at `~/missy`, launchd daily 4:45am. Verified end-to-end: SMTP send confirmed, Google Sheet read+write confirmed, Adzuna pulling (399 postings → 8 scored matches). Digest → `13dmh33@gmail.com`. |
| **Molly** | Automated + merged. Workflows Mon 6am / Sun 10pm MT, `FORCE_QUEUE=1`. Blocked on: @trevoadvisors account (does not exist), Buffer token, logo asset. |
| **Miley** | Built, merged. Blocked on Buffer token, Canva colors, product mockups. **Do not modify — separate brand.** |
| **Milly** | Live and posting. Model bug fixed. Still has no per-post fallback and no brand-validator equivalent — the only sibling posting live without either. |
| **Nora** | Config-driven core built, 46 tests. Nothing live; all gates default off. Web chat + Meta DM are stubs. |
| **Reeve** | All 6 phases built. Blocked on Meta App + Railway + Cal.com + `SERPAPI_KEY`. |
| **Merlin** | Runs locally; CI has never succeeded (§4). |
| **Strategy** | Docs claim cross-system monitoring; code reads only `reeve/output/*`. Cannot monitor Trevo. |
| **Piper** | Scope doc only, no code. Its own spec predicts the first report reads "$0 revenue, $1.28 burn, indefinite runway." |

---

## 7. Open decisions for Dave

- Install the drip launchd job? (starts real outreach)
- Add `ZOHO_EMAIL` / `ZOHO_APP_PASSWORD` as GitHub secrets to revive Merlin CI?
- Milly's hard content rules — needed before a brand-validator can be built for it.
- Push `claude/weekly-email-revenue-plan-4ysmqx` (1 commit of live pipeline data
  existing only on the Mac)?
- Reconcile or abandon `feature/post-call-proposal` (stale, missing tests `main`
  has since gained; gates the Stripe key).
