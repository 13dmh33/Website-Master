# Secrets
.env.local

# Dependencies
node_modules/

# Runtime data
# leads/*.json intentionally tracked so Diagnoser can run from container
messages/*.json
messages/*.txt
mockups/*.txt
# queue/*.json intentionally tracked so Pitcher can run on any machine

# Logs
logs/*.log

# Generated reports (local output — not tracked)
reports/*.csv
reports/*.json

# Aggregator outreach — leads/queue JSON tracked (same convention as
# leads/ and queue/ above); generated PDFs are local build output, not tracked
aggregator/pdfs/*.pdf
aggregator/leads/needs-email-*.csv

# OS
.DS_Store
AllContacts.csv
SentLog.csv
